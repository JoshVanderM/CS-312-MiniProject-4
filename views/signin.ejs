<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign In</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Sign In</h1>
        <! -- Sign In form asking for username and Password ->
        <form class="signup-signin-form" action="/signin" method="POST">
            <label for="name">Name:</label>
            <input type="text" name="name" id="name" required>

            <label for="password">Password:</label>
            <input type="password" name="password" id="password" required>

            <input type="submit" value="Sign In">
        </form>
        <! -- Error message for incorrect username and Password ->
        <% if (error) { %>
            <div class="signup-signin-error"><%= error %></div>
        <% } %>
        <! -- Allow user to make an account ->
        <div class="blog-buttons">
            <a href="/signup">
                <button type="button">Don't Have an Account? Sign Up</button>
            </a>
        </div>
    </div> 
</body>
</html>
